---
"tsp-asyncapi": patch
"tsp-asyncapi-core": patch
---

Add an internal document-context boundary for effective live graphs on the original TypeSpec Program, with scoped declaration and schema-artifact inputs and document-local resolution state. Preserve existing service selection and output defaults. Expose the necessary declaration input and per-model readers through the core unstable entry point.

Preserve security scheme declarations on cloned namespace identities without duplicating the original global registry, and discover effective operation signatures without importing provenance-only declarations.
