import{_ as t,o as s,c as i,a2 as a}from"./chunks/framework.mRCPFc5l.js";const u=JSON.parse('{"title":"HTTP and Service Bus interoperability","description":"Share an Order domain model across an HTTP API and distinct Service Bus command, event, and reply contracts.","frontmatter":{"title":"HTTP and Service Bus interoperability","description":"Share an Order domain model across an HTTP API and distinct Service Bus command, event, and reply contracts."},"headers":[],"relativePath":"guide/http-service-bus.md","filePath":"guide/http-service-bus.md","lastUpdated":1789025893000}'),n={name:"guide/http-service-bus.md"};function r(o,e,d,l,c,p){return s(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="http-and-service-bus-interoperability" tabindex="-1">HTTP and Service Bus interoperability <a class="header-anchor" href="#http-and-service-bus-interoperability" aria-label="Permalink to &quot;HTTP and Service Bus interoperability&quot;">​</a></h1><p>The public <a href="https://github.com/cataggar/tsp-asyncapi/tree/main/examples/19-http-service-bus" target="_blank" rel="noreferrer">example</a> describes three applications and the gateway&#39;s separate HTTP entrypoint. It uses the <a href="./../reference/service-bus">Service Bus companion</a> and its <a href="./../design/azure-service-bus-profile">reviewed profile</a>, not an AMQP 0-9-1 binding. There is no HTTP server, Azure SDK, provisioning, or credential acquisition here.</p><p><a href="./../zh-tw/guide/http-service-bus">繁體中文</a></p><h2 id="share-the-domain-not-every-envelope" tabindex="-1">Share the domain, not every envelope <a class="header-anchor" href="#share-the-domain-not-every-envelope" aria-label="Permalink to &quot;Share the domain, not every envelope&quot;">​</a></h2><p><code>domain/order.tsp</code> declares Order once, without HTTP, AsyncAPI, or Service Bus annotations. Its built-in constraints are shared by both schema emitters:</p><div class="language-typespec vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typespec</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">model</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Order</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  @format</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;uuid&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  orderId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  @minLength</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  customerId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  @minItems</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  items</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OrderLine</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>OrderLine has a nonempty SKU and an integer quantity between <strong>1 and 1000</strong>. The first example deliberately avoids money precision, version projections, binary formats, and read/write visibility transformations.</p><table tabindex="0"><thead><tr><th>Contract</th><th>Body</th></tr></thead><tbody><tr><td>HTTP <code>POST /orders</code> and successful <code>GET /orders/{orderId}</code></td><td>The shared Order</td></tr><tr><td>PlaceOrder command</td><td><code>commandId</code>, <code>expiresAt</code>, <code>order: Order</code></td></tr><tr><td>OrderPlaced event</td><td><code>eventId</code>, <code>occurredAt</code>, <code>expiresAt</code>, <code>order: Order</code></td></tr><tr><td>OrderCommandResult reply</td><td><code>commandId</code>, <code>orderId</code>, accepted/rejected outcome, optional reason, <code>expiresAt</code></td></tr><tr><td>HTTP 202 receipt</td><td><code>commandId</code>, <code>orderId</code>, <code>state: pending</code></td></tr></tbody></table><p>A shared source model does not mean identical HTTP/message envelopes, identical schema dialects, or automatic compatibility after evolution. Native metadata is not a property of the Order domain.</p><h2 id="flow-and-application-perspectives" tabindex="-1">Flow and application perspectives <a class="header-anchor" href="#flow-and-application-perspectives" aria-label="Permalink to &quot;Flow and application perspectives&quot;">​</a></h2><ol><li>The caller supplies an Order and UUID <code>Idempotency-Key</code>. The gateway uses that key as commandId. A runtime implementing this contract must record durable submission intent before returning <strong>202</strong>, with <code>Location: /order-submissions/{commandId}</code>. A conflicting reuse of the key produces 409. No implementation of that durability or idempotency is shipped.</li><li>The gateway sends PlaceOrder to the command queue. The processor receives it under a session keyed by orderId, records its business outcome, and sends a result to the fixed logical gateway reply queue.</li><li>On acceptance, the processor also publishes OrderPlaced to the event topic. A durable outbox/inbox or equivalent is an application design recommendation, not a transaction or workflow engine provided by these contracts.</li><li>The gateway receives the result and updates its submission/read view. <code>GET /order-submissions/{commandId}</code> returns pending/accepted/rejected; unknown records return 404. <strong>202 is not business acceptance or fulfillment.</strong></li><li>Fulfillment receives the event from its subscription. It does not receive directly from the publisher&#39;s topic endpoint, and nobody sends to a subscription. A result and event may arrive in either order.</li></ol><table tabindex="0"><thead><tr><th>Document</th><th>Operation</th><th>Primary action and endpoint</th><th>Linked reply</th></tr></thead><tbody><tr><td>Gateway</td><td><code>submitOrder</code></td><td>send to command queue</td><td>receive result</td></tr><tr><td>Gateway</td><td><code>receiveResult</code></td><td>receive from reply queue</td><td>none</td></tr><tr><td>Processor</td><td><code>processOrder</code></td><td>receive from command queue</td><td>send result</td></tr><tr><td>Processor</td><td><code>sendResult</code></td><td>send to reply queue</td><td>none</td></tr><tr><td>Processor</td><td><code>publishOrderPlaced</code></td><td>send to topic</td><td>none</td></tr><tr><td>Fulfillment</td><td><code>onOrderPlaced</code></td><td>receive from subscription</td><td>none</td></tr></tbody></table><p><code>@send op submitOrder(command: PlaceOrder): OrderCommandResult</code> describes the gateway. <code>@receive op processOrder(result: OrderCommandResult): PlaceOrder</code> inverts the signature: its return is the received request, its parameter is the sent reply. Return notation is <strong>not a synchronous call or an HTTP waiting rule</strong>.</p><p>The explicit result operations describe the <strong>same reply leg</strong>, not a second delivery. The gateway needs its own receive/settlement policy; that cannot inherit from a send operation. The processor needs an explicit send identity obligation for its duplicate-detecting reply queue. Existing <code>@replyChannel</code> links the exchange; there is no standard <code>reply.address</code> or synthetic native-header expression.</p><h2 id="native-properties-and-application-headers" tabindex="-1">Native properties and application headers <a class="header-anchor" href="#native-properties-and-application-headers" aria-label="Permalink to &quot;Native properties and application headers&quot;">​</a></h2><p><code>fixtures/flow.json</code> gives fixed, valid sample values. These are logical fixture views, not wire captures or code that automatically extracts properties:</p><table tabindex="0"><thead><tr><th>Native property</th><th>Command</th><th>Result</th><th>Event</th></tr></thead><tbody><tr><td>MessageId</td><td>commandId</td><td>distinct result ID</td><td>eventId</td></tr><tr><td>CorrelationId</td><td>commandId, by explicit conversation convention</td><td>request MessageId</td><td>originating commandId</td></tr><tr><td>SessionId</td><td>orderId</td><td>request ReplyToSessionId</td><td>orderId</td></tr><tr><td>ReplyTo</td><td>composed gateway reply queue</td><td>absent</td><td>absent</td></tr><tr><td>ReplyToSessionId</td><td>commandId</td><td>absent</td><td>absent</td></tr><tr><td>Subject</td><td><code>order.place</code></td><td><code>order.result</code></td><td><code>order.placed</code></td></tr><tr><td>ContentType</td><td><code>application/json</code></td><td><code>application/json</code></td><td><code>application/json</code></td></tr></tbody></table><p>Each logical message has a distinct ID, retained when retrying <strong>that same message</strong>. MessageId is not an operation ID. Correlation is not causation: application headers independently declare <code>traceId</code>, <code>contractVersion</code>, and an optional <code>causationId</code>; event/result fixtures set the latter to the triggering command ID. No native field is copied into headers.</p><p>The profile fixes MessageId to AMQP <code>properties/message-id</code>, CorrelationId to <code>properties/correlation-id</code>, SessionId to <code>properties/group-id</code>, ReplyTo to <code>properties/reply-to</code>, and ReplyToSessionId to <code>properties/reply-to-group-id</code>. Content type remains a standard message field. <code>ttlSeconds: 3600</code> requires the application to set AMQP <strong>header/ttl to 3,600,000 milliseconds</strong>, not an application property.</p><p>Fixed ReplyTo must agree with the composed reply channel. The responder actively routes the result there; Service Bus does not automatically execute reply routing. There is no arbitrary caller-chosen destination or extraction DSL. Correlation and session equality across messages are application obligations checked only against these fixtures—not assertions JSON Schema can enforce across deliveries.</p><h2 id="deployment-security-and-delivery-obligations" tabindex="-1">Deployment, security, and delivery obligations <a class="header-anchor" href="#deployment-security-and-delivery-obligations" aria-label="Permalink to &quot;Deployment, security, and delivery obligations&quot;">​</a></h2><p><code>environments/public.tsp</code> separates inert <code>.invalid</code> hosts and illustrative physical entity paths from logical catalog IDs. The fulfillment subscription&#39;s <code>topicId: orders.events</code> is a cross-document catalog relation, not a dangling AsyncAPI <code>$ref</code>. Do not add an unused local topic to the consumer just to hide it.</p><p>Connections require TLS, with <code>amqps</code> and protocol version <code>1.0</code>. Broker Entra ID authentication, identity selection, and role assignments stay external. The HTTP bearer requirement does not assert that its token is valid for AMQP CBS. Do not invent a standard broker bearer scheme or describe Azure RBAC roles as OAuth scopes.</p><table tabindex="0"><thead><tr><th>Application</th><th>Minimum data-plane rights at the narrowest supported scope</th></tr></thead><tbody><tr><td>Gateway</td><td>Send on commands; Listen on gateway replies</td></tr><tr><td>Processor</td><td>Listen on commands; Send on replies and event topic</td></tr><tr><td>Fulfillment</td><td>Listen on its subscription</td></tr></tbody></table><p>No control-plane permissions, client secrets, connection strings, private endpoints, or live resources are needed to compile or validate.</p><ul><li>Require Standard/Premium capabilities, sessions on queues/subscription, a 30-second lock, maximum delivery count 10, and a 24-hour entity TTL ceiling. The <strong>topic has no sessions setting</strong>; the sender supplies SessionId for consumers.</li><li>Commands, replies, and topic ingress require nonpartitioned duplicate detection with an explicit <strong>600-second window</strong>. This is bounded broker filtering, not durable business idempotency. IDs must remain stable when send outcomes are uncertain.</li><li>Primary receives explicitly require PeekLock, serial per-session processing, durable idempotency, completion after successful effects, and treating lost locks as unsettled. Lock renewal and successful settlement remain runtime work.</li><li>Transient failures are abandoned; permanent failures and exhausted deliveries need dead-letter handling. Expiration is configured for queue/subscription dead-lettering. DLQs require inspection/remediation; there is no automatic TTL cleanup or safe replay implementation.</li><li><code>expiresAt</code> is an application deadline to check, independent of broker TTL. TTL does not cancel a locked message&#39;s business effects.</li><li>There is no global ordering across sessions or destinations, unconditional eventual delivery, automatic workflow durability, or exactly-once business effect. Redelivery and duplicate processing attempts remain possible.</li></ul><h2 id="generate-and-verify" tabindex="-1">Generate and verify <a class="header-anchor" href="#generate-and-verify" aria-label="Permalink to &quot;Generate and verify&quot;">​</a></h2><p>Use Node <strong>24</strong>, pnpm <strong>11.21.0</strong>, the root frozen lockfile and workspace packages. The verified TypeSpec baseline is compiler/HTTP/OpenAPI/OpenAPI3 <strong>1.16.0</strong> and Protobuf/versioning <strong>0.86.0</strong> where used. Output versions are independently OpenAPI <strong>3.1.0</strong>, AsyncAPI <strong>3.1.0</strong>, and profile <strong>0.1.0</strong>.</p><p>The companion is <strong>unpublished</strong>. Use the workspace checkout until its coordinated release with a core version containing the required extension APIs, then pin actual published versions. A package manifest does not establish release availability.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --frozen-lockfile</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples:interop</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples:interop:check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vitest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/integration/http-service-bus-example.test.ts</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/integration/http-service-bus-payloads.test.ts</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs:build</span></span></code></pre></div><p>All four entrypoints have their own config. The root is the gateway messaging default; <code>/http</code>, <code>/processor</code>, and <code>/fulfillment</code> are independent projects. The generator reads the real configs and captures eight outputs in memory before writing anything. Check mode compares exact bytes and rejects missing/unexpected artifacts; it never rewrites baselines. No timestamps or random IDs are generated.</p><p>The existing CI Vitest invocation runs the generation check, official AsyncAPI parser, offline OpenAPI validator, local-reference and profile checks, and actual valid/invalid payload/header/native-fixture controls. HTTP uses OpenAPI/2020-12; messaging uses the shared <code>createPayloadValidator</code> and <code>createMessageValidator</code><strong>Draft-07</strong> lane, with explicit formats and no coercion, defaults, or field removal. These helpers return acceptance and string diagnostics; HTTP remains on its own reviewed OpenAPI/2020-12 validator, not a substituted messaging oracle. Native properties remain separate fixture checks. Other schema languages and evolution scenarios need additional evidence beyond this selected native JSON example. Samples originating from HTTP, commands, and events are checked against both sides; complete envelopes are also tested so wrong-level extraction cannot pass unnoticed.</p><p>The known <code>deployment-unverified</code> warning is expected and preserved. Static profile/schema validation is not Azure deployment verification, runtime security, settlement, deduplication, or proof of universal producer/consumer compatibility.</p>`,33)])])}const m=t(n,[["render",r]]);export{u as __pageData,m as default};
