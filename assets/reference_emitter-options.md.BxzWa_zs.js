import{_ as t,o as a,c as s,a2 as i}from"./chunks/framework.mRCPFc5l.js";const u=JSON.parse('{"title":"Emitter Options","description":"Set these in `tspconfig.yaml`, or pass them on the CLI with `--option \\"tsp-asyncapi.<name>=<value>\\"`.","frontmatter":{"title":"Emitter Options","description":"Set these in `tspconfig.yaml`, or pass them on the CLI with `--option \\"tsp-asyncapi.<name>=<value>\\"`."},"headers":[],"relativePath":"reference/emitter-options.md","filePath":"reference/emitter-options.md","lastUpdated":1789019229000}'),o={name:"reference/emitter-options.md"};function n(r,e,d,c,l,p){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="emitter-options" tabindex="-1">Emitter Options <a class="header-anchor" href="#emitter-options" aria-label="Permalink to &quot;Emitter Options&quot;">​</a></h1><p>Set these in <code>tspconfig.yaml</code>, or pass them on the CLI with <code>--option &quot;tsp-asyncapi.&lt;name&gt;=&lt;value&gt;&quot;</code>.</p><table tabindex="0"><thead><tr><th>Option</th><th>Type</th><th>Default</th><th>Effect</th></tr></thead><tbody><tr><td><code>file-type</code></td><td><code>&quot;yaml&quot; | &quot;json&quot;</code></td><td><code>yaml</code></td><td>Serialization format of the document.</td></tr><tr><td><code>output-file</code></td><td><code>string</code></td><td><code>asyncapi.{service-name-if-multiple}.{version}.{file-type}</code></td><td>Filename or template, written under <code>tsp-output/tsp-asyncapi/</code>.</td></tr><tr><td><code>service</code></td><td><code>string</code></td><td>(all declared services)</td><td>Select one exact fully qualified service namespace, such as <code>Company.Orders</code>.</td></tr><tr><td><code>asyncapi-id</code></td><td><code>string</code></td><td>(omitted)</td><td>Emitted as the document&#39;s top-level <code>id</code> field — the application&#39;s global identifier, conventionally a URN.</td></tr><tr><td><code>default-content-type</code></td><td><code>string</code></td><td>(omitted)</td><td>Emitted as <code>defaultContentType</code> — the content type message payloads use when a message declares none.</td></tr><tr><td><code>preview-features</code></td><td><code>string[]</code></td><td><code>[]</code></td><td>Turns on preview features. The reserved names are <code>protobuf</code> and <code>avro</code>.</td></tr></tbody></table><p>Options the schema declares but you don&#39;t set are omitted from the document entirely, not emitted as empty values.</p><h2 id="via-tspconfig-yaml" tabindex="-1">Via <code>tspconfig.yaml</code> <a class="header-anchor" href="#via-tspconfig-yaml" aria-label="Permalink to &quot;Via \`tspconfig.yaml\`&quot;">​</a></h2><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">emit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;tsp-asyncapi&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">options</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  &quot;tsp-asyncapi&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    output-file</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;orders.yaml&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    file-type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;yaml&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    asyncapi-id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;urn:com:example:orders&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    default-content-type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;application/json&quot;</span></span></code></pre></div><h2 id="via-the-cli" tabindex="-1">Via the CLI <a class="header-anchor" href="#via-the-cli" aria-label="Permalink to &quot;Via the CLI&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tsp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compile</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --emit</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tsp-asyncapi</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --option</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;tsp-asyncapi.file-type=json&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --option</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;tsp-asyncapi.asyncapi-id=urn:com:example:orders&quot;</span></span></code></pre></div><p>An unknown option name fails validation (<code>additionalProperties: false</code>), so a typo is caught at compile time rather than silently ignored. A name in <code>preview-features</code> that is not reserved fails the same way, and the message lists the names that are.</p><h2 id="service-documents-and-filenames" tabindex="-1">Service documents and filenames <a class="header-anchor" href="#service-documents-and-filenames" aria-label="Permalink to &quot;Service documents and filenames&quot;">​</a></h2><p>By default, every namespace marked <code>@service</code> produces its own AsyncAPI document. This includes HTTP-only services: a service without messaging declarations produces an empty AsyncAPI document, rather than being guessed from HTTP decorators. No HTTP library is required.</p><p><code>service</code> matches the namespace&#39;s exact, case-sensitive fully qualified name, not its title or short name. An unknown or ambiguous selector is an error. Selecting <code>Company.Orders</code> from a program with several services still writes <code>asyncapi.Company.Orders.yaml</code>; filtering never changes the original service count used for naming.</p><table tabindex="0"><thead><tr><th>Token</th><th>Value</th></tr></thead><tbody><tr><td><code>{service-name}</code></td><td>Original fully qualified service namespace; omitted for the no-service fallback.</td></tr><tr><td><code>{service-name-if-multiple}</code></td><td>The same name, only when the original program declares more than one service.</td></tr><tr><td><code>{version}</code></td><td>Optional version identity supplied by a version-aware adapter; omitted for unversioned documents. This option alone does not enable TypeSpec versioning.</td></tr><tr><td><code>{file-type}</code></td><td><code>yaml</code> or <code>json</code>.</td></tr></tbody></table><p>An omitted token also removes its following <code>.</code> or <code>/</code>. Thus the default remains <code>asyncapi.yaml</code> or <code>asyncapi.json</code> with zero or one unversioned service. A template such as <code>{service-name}/asyncapi.{file-type}</code> creates a service subdirectory. Service/version values are escaped as portable path segments: UTF-8 percent encoding preserves punctuation and Unicode without allowing path separators, reserved device names, or trailing dots to change the destination.</p><p>A literal <code>output-file: orders.yaml</code> remains valid for one selected document. For several documents, include distinguishing tokens. All paths are resolved and checked together, including case-insensitive collisions. Unknown tokens, invalid filenames, and collisions are errors; the emitter does not overwrite one document with another or append counters.</p><h2 id="ownership-and-reusable-contracts" tabindex="-1">Ownership and reusable contracts <a class="header-anchor" href="#ownership-and-reusable-contracts" aria-label="Permalink to &quot;Ownership and reusable contracts&quot;">​</a></h2><p>A declaration belongs to its nearest enclosing <code>@service</code>. Nested services are separate boundaries, even when their namespaces are reopened elsewhere. Channels, actions, messages, replies, servers, security schemes, bindings, tags, extensions, and document diagnostics are resolved inside that boundary before assigning component keys.</p><p>Owned <code>@message</code> models are retained even when unused. A messaging signature may explicitly use an unowned reusable <code>@message</code>; unreferenced unowned messages stay out when there are multiple services. Ordinary domain models may be shared through payloads and headers, including models declared under another service, without importing that service&#39;s application metadata. A foreign service&#39;s <code>@message</code> envelope or reply channel is an error: wrap shared domain data in a local envelope instead. This includes foreign envelopes reached through discriminator-derived subtypes; ordinary undecorated shared subtypes remain valid.</p><p>With one original service, otherwise unowned declarations keep their legacy implicit ownership. With no services, the legacy global fallback document remains. With multiple original services, unowned channels and actions are ambiguous, even if <code>service</code> selects just one app. Move application declarations beneath their owner. Unowned inherited/template operation signatures that are realized on an owned channel are reusable carriers, not additional application roots.</p><p>The emitter resolves all selected documents before writing any of them. New selection/ownership errors, output collisions, ambiguous visible security definitions, and provider refusals withhold the whole output set. Existing resolve/lower diagnostic-and-drop behavior remains unchanged. <code>noEmit</code> suppresses writes, not diagnostics. Source validation still applies to the entire TypeSpec program; selecting a service does not hide source errors in another service.</p><p>Changed-graph adapters must supply a complete live declaration boundary, including retained alias-only messages, channels, and actions. Namespace-map discovery alone cannot prove that boundary complete. Missing or stale inputs report <code>incomplete-effective-document</code> or <code>stale-effective-declaration</code> and refuse the context. Original/source models are diagnostics or inventory inputs, not substitutes for live declarations or artifacts. An explicitly identical original graph retains the ordinary alias behavior.</p><h2 id="preview-features" tabindex="-1">Preview features <a class="header-anchor" href="#preview-features" aria-label="Permalink to &quot;Preview features&quot;">​</a></h2><p>A preview feature changes the emitted document. Two names are reserved: <code>protobuf</code> and <code>avro</code>. Both work in this release. A name with no provider behind it reports <code>preview-feature-unavailable</code>, and no file is written. A request that names one working feature and one unavailable one is refused the same way, because the request as a whole cannot be answered.</p><p><code>protobuf</code> gives a model that carries the official <code>TypeSpec.Protobuf</code> decorators a proto3 payload. The <a href="./../guide/protobuf-payloads">Protobuf payloads guide</a> shows what it writes.</p><p><code>avro</code> gives a model that carries the <code>tsp-avro</code> <code>@Avro.avroRecord</code> decorator an Avro payload. The payload is written as an object, because Avro is JSON. <code>tsp-avro</code> is an optional peer dependency of the emitter. Install it in the project that turns this feature on.</p><p>Both features can be on at once. One model may carry only one of the two sets of decorators. A model that carries both is reported as <code>conflicting-generated-schema-source</code>, and no file is written.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Nothing is emitted when a preview feature is refused. A document written next to the error would ignore the request without saying so.</p></div>`,27)])])}const m=t(o,[["render",n]]);export{u as __pageData,m as default};
