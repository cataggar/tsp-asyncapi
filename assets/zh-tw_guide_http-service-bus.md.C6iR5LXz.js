import{_ as t,o as s,c as d,a2 as i}from"./chunks/framework.mRCPFc5l.js";const k=JSON.parse('{"title":"HTTP 與 Service Bus 互通","description":"讓 HTTP API、Service Bus command、event 與 reply 共用 Order 領域 model，同時保留不同封裝。","frontmatter":{"title":"HTTP 與 Service Bus 互通","description":"讓 HTTP API、Service Bus command、event 與 reply 共用 Order 領域 model，同時保留不同封裝。"},"headers":[],"relativePath":"zh-tw/guide/http-service-bus.md","filePath":"zh-tw/guide/http-service-bus.md","lastUpdated":1789025893000}'),a={name:"zh-tw/guide/http-service-bus.md"};function r(o,e,n,p,l,c){return s(),d("div",null,[...e[0]||(e[0]=[i(`<h1 id="http-與-service-bus-互通" tabindex="-1">HTTP 與 Service Bus 互通 <a class="header-anchor" href="#http-與-service-bus-互通" aria-label="Permalink to &quot;HTTP 與 Service Bus 互通&quot;">​</a></h1><p>公開<a href="https://github.com/cataggar/tsp-asyncapi/tree/main/examples/19-http-service-bus" target="_blank" rel="noreferrer">範例</a> 描述三個應用程式及 gateway 獨立的 HTTP 進入點，使用 <a href="./../reference/service-bus">Service Bus companion</a> 與 <a href="./../design/azure-service-bus-profile">已審查的 profile</a>，不是 AMQP 0-9-1 binding。 範例沒有 HTTP server、Azure SDK、佈建或憑證取得程式。</p><p><a href="./../../guide/http-service-bus">English</a></p><h2 id="共用領域-不強迫共用封裝" tabindex="-1">共用領域，不強迫共用封裝 <a class="header-anchor" href="#共用領域-不強迫共用封裝" aria-label="Permalink to &quot;共用領域，不強迫共用封裝&quot;">​</a></h2><p><code>domain/order.tsp</code> 只宣告一次 Order，不含 HTTP、AsyncAPI 或 Service Bus annotation。兩種 emitter 共用下列內建限制：</p><div class="language-typespec vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typespec</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">model</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Order</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  @format</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;uuid&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  orderId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  @minLength</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  customerId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  @minItems</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  items</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OrderLine</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>OrderLine 的 SKU 不可為空，quantity 必須是 <strong>1 到 1000</strong> 的整數。本範例刻意 不涵蓋金額精度、版本投影、二進位格式或讀寫 visibility 轉換。</p><table tabindex="0"><thead><tr><th>契約</th><th>Body</th></tr></thead><tbody><tr><td>HTTP <code>POST /orders</code>、成功的 <code>GET /orders/{orderId}</code></td><td>共用的 Order</td></tr><tr><td>PlaceOrder command</td><td><code>commandId</code>、<code>expiresAt</code>、<code>order: Order</code></td></tr><tr><td>OrderPlaced event</td><td><code>eventId</code>、<code>occurredAt</code>、<code>expiresAt</code>、<code>order: Order</code></td></tr><tr><td>OrderCommandResult reply</td><td><code>commandId</code>、<code>orderId</code>、accepted/rejected、選用 reason、<code>expiresAt</code></td></tr><tr><td>HTTP 202 回條</td><td><code>commandId</code>、<code>orderId</code>、<code>state: pending</code></td></tr></tbody></table><p>共用原始 model 不代表 HTTP/message 封裝相同、schema 方言相同，或演進後自動 相容。Native metadata 不是 Order 領域的屬性。</p><h2 id="流程與應用程式視角" tabindex="-1">流程與應用程式視角 <a class="header-anchor" href="#流程與應用程式視角" aria-label="Permalink to &quot;流程與應用程式視角&quot;">​</a></h2><ol><li>Caller 提交 Order 與 UUID <code>Idempotency-Key</code>；gateway 用它當 commandId。 實作必須先持久記錄提交意圖，才回傳 <strong>202</strong> 與 <code>Location: /order-submissions/{commandId}</code>。相同 key 的衝突內容回傳 409。 本範例沒有實作這些持久化或冪等性機制。</li><li>Gateway 把 PlaceOrder 送到 command queue。Processor 在 orderId session 收取 command、記錄業務結果，再送結果到固定的 gateway reply queue。</li><li>接受訂單時，processor 另外發佈 OrderPlaced 到 topic。持久化 outbox/inbox 或同等設計是應用責任，不是這份契約提供的交易或工作流程引擎。</li><li>Gateway 收到 reply 後更新提交與查詢狀態。 <code>GET /order-submissions/{commandId}</code> 回傳 pending/accepted/rejected，未知紀錄 回傳 404。<strong>202 不代表訂單已被業務接受，更不代表已完成履約。</strong></li><li>Fulfillment 從自己的 subscription 收 event，不直接從 publisher 的 topic 端點接收；任何應用都不會把 subscription 當成 send 目的地。Reply 與 event 的到達順序不受保證。</li></ol><table tabindex="0"><thead><tr><th>文件</th><th>Operation</th><th>主要動作與端點</th><th>關聯 reply</th></tr></thead><tbody><tr><td>Gateway</td><td><code>submitOrder</code></td><td>send 到 command queue</td><td>receive result</td></tr><tr><td>Gateway</td><td><code>receiveResult</code></td><td>receive reply queue</td><td>無</td></tr><tr><td>Processor</td><td><code>processOrder</code></td><td>receive command queue</td><td>send result</td></tr><tr><td>Processor</td><td><code>sendResult</code></td><td>send 到 reply queue</td><td>無</td></tr><tr><td>Processor</td><td><code>publishOrderPlaced</code></td><td>send 到 topic</td><td>無</td></tr><tr><td>Fulfillment</td><td><code>onOrderPlaced</code></td><td>receive subscription</td><td>無</td></tr></tbody></table><p>Gateway 的 <code>@send op submitOrder(command: PlaceOrder): OrderCommandResult</code>， 與 processor 的 <code>@receive op processOrder(result: OrderCommandResult): PlaceOrder</code> 簽章方向相反。Receive 的回傳型別是收到的 request，參數是送出的 reply。 回傳型別<strong>不表示同步呼叫，也不表示 HTTP 必須等待 broker reply</strong>。</p><p>明確列出的 result operation 是<strong>同一條 reply 路徑</strong>，不是第二次傳送。Gateway 需要自己的 receive/settlement 需求，不能從 send 繼承；processor 的 duplicate-detecting reply queue 需要明確的 send identity 義務。既有 <code>@replyChannel</code> 關聯交換，沒有標準 <code>reply.address</code> 或虛構的 native-header 表達式。</p><h2 id="native-property-與-application-header" tabindex="-1">Native property 與 application header <a class="header-anchor" href="#native-property-與-application-header" aria-label="Permalink to &quot;Native property 與 application header&quot;">​</a></h2><p><code>fixtures/flow.json</code> 提供固定且有效的值。這些是邏輯 fixture 視圖，不是封包擷取 或會自動抽取屬性的程式：</p><table tabindex="0"><thead><tr><th>Native property</th><th>Command</th><th>Result</th><th>Event</th></tr></thead><tbody><tr><td>MessageId</td><td>commandId</td><td>獨立 result ID</td><td>eventId</td></tr><tr><td>CorrelationId</td><td>明確約定為 commandId</td><td>request MessageId</td><td>起因 commandId</td></tr><tr><td>SessionId</td><td>orderId</td><td>request ReplyToSessionId</td><td>orderId</td></tr><tr><td>ReplyTo</td><td>組合後的 gateway reply queue</td><td>無</td><td>無</td></tr><tr><td>ReplyToSessionId</td><td>commandId</td><td>無</td><td>無</td></tr><tr><td>Subject</td><td><code>order.place</code></td><td><code>order.result</code></td><td><code>order.placed</code></td></tr><tr><td>ContentType</td><td><code>application/json</code></td><td><code>application/json</code></td><td><code>application/json</code></td></tr></tbody></table><p>每一個邏輯 message 都有不同 ID，重試<strong>同一個 message</strong> 時才保留它。MessageId 不是 operation ID。Correlation 不等於 causation：application header 另外宣告 <code>traceId</code>、<code>contractVersion</code> 與選用的 <code>causationId</code>；event/result fixture 的 causationId 是觸發它的 command ID。Native property 不會被複製到 header。</p><p>Profile 固定 MessageId 對應 AMQP <code>properties/message-id</code>，CorrelationId 對應 <code>properties/correlation-id</code>，SessionId 對應 <code>properties/group-id</code>，ReplyTo 對應 <code>properties/reply-to</code>，ReplyToSessionId 對應 <code>properties/reply-to-group-id</code>。 Content type 保留在標準 message 欄位。<code>ttlSeconds: 3600</code> 要求應用把 AMQP <strong>header/ttl 設為 3,600,000 毫秒</strong>，不是 application property。</p><p>固定 ReplyTo 必須符合組合後的 reply channel。Responder 必須主動路由 reply； Service Bus 不會自動執行 request/reply。這不授權任意 caller 指定目的地，也 不是 extraction DSL。跨 message 的 correlation/session 相等關係是應用義務； 測試只檢查 fixture，JSON Schema 不能跨實際 delivery 強制這些關係。</p><h2 id="部署、安全與傳送責任" tabindex="-1">部署、安全與傳送責任 <a class="header-anchor" href="#部署、安全與傳送責任" aria-label="Permalink to &quot;部署、安全與傳送責任&quot;">​</a></h2><p><code>environments/public.tsp</code> 把 <code>.invalid</code> host 與示意 physical path 和 logical ID 分開。Subscription 的 <code>topicId: orders.events</code> 是跨文件 catalog 關係，不是懸空 AsyncAPI <code>$ref</code>。Consumer 不必加入不使用的 topic channel 來掩蓋這個關係。</p><p>連線要求 TLS、<code>amqps</code> 與 protocol version <code>1.0</code>。Broker Entra ID 驗證、身分選擇 與角色指派是外部設定。HTTP bearer 需求不代表同一 token 可用於 AMQP CBS； 不要虛構 broker bearer scheme，也不要把 Azure RBAC role 當成 OAuth scope。</p><table tabindex="0"><thead><tr><th>應用程式</th><th>在最小可支援範圍內的 data-plane 權限</th></tr></thead><tbody><tr><td>Gateway</td><td>command 的 Send；gateway reply 的 Listen</td></tr><tr><td>Processor</td><td>command 的 Listen；reply/topic 的 Send</td></tr><tr><td>Fulfillment</td><td>自己 subscription 的 Listen</td></tr></tbody></table><p>編譯及驗證不需要 control-plane 權限、client secret、connection string、私人端點 或真實資源。</p><ul><li>需求為 Standard/Premium、queue/subscription session、30 秒 lock、最多 10 次 broker delivery，以及 24 小時 entity TTL 上限。<strong>Topic 沒有 sessions 設定</strong>； publisher 為下游 consumer 提供 SessionId。</li><li>Command、reply 與 topic ingress 要求非分割的 duplicate detection，window 明確 設為 <strong>600 秒</strong>。這是有限時間的 broker 過濾，不是持久的業務冪等性；send 結果 不確定時，重試仍要保留同一 message ID。</li><li>主要 receive 明確要求 PeekLock、session 內循序處理、持久冪等性、效果成功後 complete，以及 lock loss 當成未 settlement。Lock renewal 與 settlement 結果處理仍由執行中的應用負責。</li><li>暫時失敗用 abandon；永久錯誤或耗盡 delivery 要處理 dead letter。 Queue/subscription 另外設定過期 dead-lettering。DLQ 需要調查與補救，不會自動 依 TTL 清除，也沒有本範例提供的安全重播程式。</li><li><code>expiresAt</code> 是獨立於 broker TTL 的應用 deadline。TTL 不會取消已鎖定 message 的業務效果。</li><li>不保證跨 session/目的地的全域順序、無條件最終傳送、自動工作流程持久性或 exactly-once 業務效果。Redelivery 與重複處理嘗試仍可能發生。</li></ul><h2 id="產生與驗證" tabindex="-1">產生與驗證 <a class="header-anchor" href="#產生與驗證" aria-label="Permalink to &quot;產生與驗證&quot;">​</a></h2><p>使用 Node <strong>24</strong>、pnpm <strong>11.21.0</strong>、根目錄 frozen lockfile 與 workspace package。 已驗證的 TypeSpec 工具鏈為 compiler/HTTP/OpenAPI/OpenAPI3 <strong>1.16.0</strong>， workspace 使用的 Protobuf/versioning 為 <strong>0.86.0</strong>。輸出版本另為 OpenAPI <strong>3.1.0</strong>、AsyncAPI <strong>3.1.0</strong>、profile <strong>0.1.0</strong>。</p><p>Companion <strong>尚未發布</strong>，目前請使用 workspace checkout。它必須與含必要 extension API 的 core 版本協調發布，再固定真正發布的版本；package manifest 不代表該版本已可安裝。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --frozen-lockfile</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples:interop</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples:interop:check</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> vitest</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/integration/http-service-bus-example.test.ts</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/integration/http-service-bus-payloads.test.ts</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs:build</span></span></code></pre></div><p>四個進入點各有設定。根目錄預設是 gateway messaging；<code>/http</code>、<code>/processor</code>、 <code>/fulfillment</code> 各自獨立。Generator 讀取真正的設定，先在記憶體收集八份輸出才 寫入。Check 模式比對原始位元組與檔案集合，缺少或多出文件會失敗，不改寫 baseline， 也不產生隨機 ID 或現在時間。</p><p>既有 CI 的 Vitest 會跑生成檢查、官方 AsyncAPI parser、離線 OpenAPI validator、 本機 reference/profile 檢查，以及真正的有效/無效 payload、header 與 native fixture。HTTP 用 OpenAPI/2020-12，訊息用共用 <code>createPayloadValidator</code> 與 <code>createMessageValidator</code> 的 <strong>Draft-07</strong> 驗證路徑，明確註冊 format，不轉型、 不加 default、不移除欄位。這些 helper 回傳接受結果與字串診斷；HTTP 仍用自己的 已審查 validator，不拿它取代訊息方言。Native property 另做 fixture 檢查； 其他 schema 語言或演進情境需要額外證據。來自 HTTP/command/event 的 Order 會交叉驗證，也會驗證整個封裝，避免取錯 payload 層級卻仍通過。</p><p><code>deployment-unverified</code> 是預期且保留的警告。靜態 profile/schema 驗證不等於 Azure 部署、安全、settlement、deduplication 或通用 producer/consumer 相容性證明。</p>`,33)])])}const u=t(a,[["render",r]]);export{k as __pageData,u as default};
