import{_ as t,o as i,c as s,a2 as a}from"./chunks/framework.mRCPFc5l.js";const u=JSON.parse('{"title":"Proposed Azure Service Bus profile","description":"RFC for a closed, versioned AsyncAPI 3.1.0 Service Bus contract profile, without a runtime or provisioning API.","frontmatter":{"title":"Proposed Azure Service Bus profile","description":"RFC for a closed, versioned AsyncAPI 3.1.0 Service Bus contract profile, without a runtime or provisioning API."},"headers":[],"relativePath":"design/azure-service-bus-profile.md","filePath":"design/azure-service-bus-profile.md","lastUpdated":1789024301000}'),n={name:"design/azure-service-bus-profile.md"};function o(r,e,d,c,l,p){return i(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="azure-service-bus-contract-profile" tabindex="-1">Azure Service Bus contract profile <a class="header-anchor" href="#azure-service-bus-contract-profile" aria-label="Permalink to &quot;Azure Service Bus contract profile&quot;">​</a></h1><p><strong>Status: reviewed for initial implementation, 2026-09-10.</strong> This is the design deliverable for <a href="https://github.com/cataggar/tsp-asyncapi/issues/5" target="_blank" rel="noreferrer">#5</a>. Independent automated design review of <a href="https://github.com/cataggar/tsp-asyncapi/pull/7" target="_blank" rel="noreferrer">#7</a> found no blocking correctness issues; the implementation coordinator accepted the design under the requested implementation and serial-merge workflow. This records the implementation design gate, not human maintainer signoff or approval to release companion APIs. Profile <code>0.1.0</code> is not an official Azure or AsyncAPI binding.</p><p><a href="./../zh-tw/design/azure-service-bus-profile">繁體中文</a> | <a href="/profiles/azure-service-bus/0.1.0/schema.json">Normative JSON Schema</a> | <a href="#examples">Example contracts</a> | <a href="#review-gate">Review gate</a></p><h2 id="_1-scope-and-compatibility" tabindex="-1">1. Scope and compatibility <a class="header-anchor" href="#_1-scope-and-compatibility" aria-label="Permalink to &quot;1. Scope and compatibility&quot;">​</a></h2><p>This RFC defines <strong>contract requirements</strong>, not SDK options or observed deployment facts. A conforming implementation preserves the broker-neutral core and writes one <code>x-azure-service-bus</code> value per target. The extension schema and the cross-object rules below are jointly normative; JSON Schema alone cannot validate topology, standard fields, application code or Azure resources. &quot;Must&quot; describes conformance to this proposed profile, not a new promise from Azure.</p><table tabindex="0"><thead><tr><th>Layer</th><th>Pinned baseline / proposed support</th></tr></thead><tbody><tr><td>AsyncAPI document</td><td>Exactly <code>3.1.0</code>; not a floating <code>3.x</code> target [A1]</td></tr><tr><td>TypeSpec compiler</td><td><code>1.16.0</code></td></tr><tr><td>Core / emitter inspected</td><td><code>tsp-asyncapi-core</code> <code>0.4.2</code> / <code>tsp-asyncapi</code> <code>0.7.2</code>, repository <code>022e4bb</code> [R1]</td></tr><tr><td>Reproduction</td><td>Node <strong>24</strong>, pnpm <strong>11.21.0</strong>; compiler and workspace require Node &gt;=22</td></tr><tr><td>Planned versioning compatibility</td><td><code>@typespec/versioning</code> <code>0.86.0</code> with compiler <code>1.16.0</code>; no versioned profile conformance claim until #1 integration below</td></tr><tr><td>Extension profile</td><td>Proposed <code>profileVersion: &quot;0.1.0&quot;</code>; schema dialect Draft-07</td></tr><tr><td>Payload/header dialect</td><td>AsyncAPI 3.1.0 native Schema Object (Draft-07-based); initial companion conformance covers native JSON only</td></tr><tr><td>Future companion/core releases</td><td>Select and pin the <strong>actual released versions containing the approved APIs</strong> when available; core 0.4.2 has no public extension writer. No future package version is assigned here.</td></tr></tbody></table><p>The original design review used compiler <code>1.15.0</code> and versioning <code>0.85.0</code>; the implementation baseline advances to the compatible <code>1.16.0</code>/<code>0.86.0</code> release pair to address the compiler dependency audit. The historical source revisions below remain unchanged.</p><p>Application <code>info.version</code>, a message&#39;s schema version, TypeSpec/library versions, AsyncAPI version and <code>profileVersion</code> are independent. The schema <code>$id</code> is its intended documentation address, not a claim that this proposed version has been published.</p><p>Initial scope is separate, unversioned application entrypoints, queues/topics/subscriptions, AMQP 1.0 over TLS, portable application properties, native string metadata, fixed or explicitly constrained native reply routing, and declared delivery requirements. No runtime, Azure SDK, provisioning, credential acquisition, HTTP streaming, HTTP LRO/status polling, scheduler, workflow engine, distributed transaction coordination or automatic compatibility proof is introduced. <strong>Neither exactly-once business effects nor automatic workflow durability is promised.</strong></p><h2 id="_2-broker-neutral-contract-first" tabindex="-1">2. Broker-neutral contract first <a class="header-anchor" href="#_2-broker-neutral-contract-first" aria-label="Permalink to &quot;2. Broker-neutral contract first&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Intent</th><th>Existing core / AsyncAPI representation</th><th>Service Bus interpretation</th></tr></thead><tbody><tr><td>Command/event</td><td><code>@message</code> model, optionally <code>messageKind</code> below</td><td>Intent, not a transport primitive. Queue commands and topic events are recommended patterns, not restrictions.</td></tr><tr><td>Application behavior</td><td><code>@send</code> / <code>@receive</code>, standard <code>operation.action</code></td><td>Relative to the application described, never auto-invert another application&#39;s document</td></tr><tr><td>Destination/source</td><td><code>@channel</code> or <code>@dynamicChannel</code>, standard channel references</td><td>Logical entity relation in the profile; physical address only in composition</td></tr><tr><td>Body</td><td>Message <code>payload</code> and native schema</td><td>Application serializes/deserializes bytes; broker treats the body as opaque</td></tr><tr><td>Application properties</td><td><code>@headers(Model)</code> or top-level <code>@header</code>, message <code>headers</code></td><td>AMQP <code>application-properties</code> only; portable subset below</td></tr><tr><td>Content type</td><td>Existing <code>@contentType</code>, message <code>contentType</code></td><td>Single source for native <code>properties/content-type</code></td></tr><tr><td>Correlation</td><td>Existing <code>@correlationId</code> only for a real payload/application-property location</td><td>Native-only correlation remains extension metadata</td></tr><tr><td>Reply</td><td><code>@replyChannel</code>, standard <code>reply.channel</code> / <code>reply.messages</code></td><td>Reply relationships and native routing obligations below</td></tr><tr><td>Security</td><td>Standard servers, schemes and security references when accurate</td><td>TLS, identity and data-plane authorization still require composition and runtime implementation</td></tr></tbody></table><p>For a gateway, <code>@send op place(command: PlaceOrder): OrderResult</code> sends the request and receives replies. For its processor, <code>@receive op process(result: OrderResult): PlaceOrder</code> receives the request and sends replies. These signatures do not make execution synchronous. A publisher sends to a topic; a fulfillment application receives from a subscription whose <code>topicId</code> names that topic even if the topic is absent from its own channels. Queues support either action. This profile rejects send-to-subscription and receive-from-topic [A1, Z1].</p><h3 id="reuse-decisions" tabindex="-1">Reuse decisions <a class="header-anchor" href="#reuse-decisions" aria-label="Permalink to &quot;Reuse decisions&quot;">​</a></h3><table tabindex="0"><thead><tr><th>TypeSpec facility</th><th>Decision for the first companion</th></tr></thead><tbody><tr><td><code>@typespec/events</code></td><td>No new dependency. <code>@events</code> unions, <code>@data</code> payload selection and <code>@contentType</code> on variants/properties differ from existing model-level message semantics. An optional broker-neutral adapter can follow; <code>unsafe_getEventDefinitions</code> is experimental, not a stable replacement. [T1]</td></tr><tr><td><code>@typespec/streams</code></td><td>No dependency. <code>@streamOf</code> describes a stream protocol model, not broker settlement, subscription topology or durable session state. HTTP/SSE streaming remains separate. [T1]</td></tr><tr><td><code>@typespec/json-schema</code></td><td>Reuse through a deliberate provider/multi-format route later, not by copying internals or placing Draft 2020-12 keywords into native Draft-07 schemas. #3 owns fidelity and conversion evidence. [T1, A1]</td></tr><tr><td><code>@typespec/versioning</code></td><td>Reuse snapshots and <code>@useDependency</code>, not a second version graph. Initial companion rejects versioned/dependency-mutated profile use until scoped replay and relational diagnostics are integrated with #1/#4. [T1]</td></tr></tbody></table><h2 id="_3-normative-profile-interface" tabindex="-1">3. Normative profile interface <a class="header-anchor" href="#_3-normative-profile-interface" aria-label="Permalink to &quot;3. Normative profile interface&quot;">​</a></h2><p>The <a href="/profiles/azure-service-bus/0.1.0/schema.json">schema</a> validates <strong>one extension value</strong>. Its named definitions <code>info</code>, <code>channel</code>, <code>message</code>, <code>operation</code> are also placement-specific entrypoints (<code>#/definitions/channel</code>, for example). Every object is closed: unknown members are errors, not ignored future options. No <code>default</code>, coercion, inferred feature enablement or deep merge is allowed. An omitted optional member means <strong>unspecified</strong>, not false, zero, &quot;latest&quot;, disabled, or guaranteed.</p><p>Every value has required <code>profileVersion: &quot;0.1.0&quot;</code> and <code>target: &quot;info&quot; | &quot;channel&quot; | &quot;message&quot; | &quot;operation&quot;</code>. <code>target</code> must match the actual emitted object, including objects promoted into <code>components</code>; it is not permission to choose a new placement.</p><table tabindex="0"><thead><tr><th>Target</th><th>Required additional fields</th><th>Optional fields</th></tr></thead><tbody><tr><td>Service namespace -&gt; <code>info</code></td><td><code>asyncapiVersion: &quot;3.1.0&quot;</code></td><td>None</td></tr><tr><td>Channel interface/namespace -&gt; channel</td><td><code>entity</code></td><td><code>deploymentRequirements</code></td></tr><tr><td>Marked <code>@message</code> model -&gt; message</td><td>None</td><td><code>messageKind</code>, <code>nativeProperties</code>, <code>ttlSeconds</code></td></tr><tr><td><code>@send</code> / <code>@receive</code> operation -&gt; operation</td><td>\`action: &quot;send&quot;</td><td>&quot;receive&quot;\` matching standard action</td></tr></tbody></table><p>Each profile-bearing application document must have the info declaration. Every channel, operation and message participating in its Service Bus paths, including replies, must carry its matching profile; unrelated broker-neutral paths need not. Profiles do not inherit from info, a channel, an operation or a message. A service namespace must not also be a profile-bearing channel: the current generic extension resolver copies its value to both placements.</p><p><strong>Only these four placements are supported.</strong> No root, server, payload-schema, property, reply-object or trait placement is defined. Server objects still use standard protocol and security fields. Do not emit <code>amqp</code> (AMQP 0-9-1) bindings on a profiled path. Every standard <code>amqp1</code> binding object, including server/message/operation/channel bindings and referenced binding components, must be <strong>absent (recommended) or <code>{}</code></strong>; even <code>bindingVersion</code> is forbidden by the current AMQP 1.0 binding revision [A2]. Other transport bindings on those paths are unsupported by this profile.</p><h3 id="channel-config-and-topology" tabindex="-1">Channel config and topology <a class="header-anchor" href="#channel-config-and-topology" aria-label="Permalink to &quot;Channel config and topology&quot;">​</a></h3><p><code>entity</code> is one of <code>{kind: &quot;queue&quot;, id}</code>, <code>{kind: &quot;topic&quot;, id}</code>, or <code>{kind: &quot;subscription&quot;, id, topicId}</code>. IDs match <code>^[a-z][a-z0-9]*(?:[.-][a-z0-9]+)*$</code>, at most 128 characters. This is a project identifier convention, <strong>not an Azure entity-name limit</strong>.</p><p>IDs identify entities within a shared logical composition/catalog, not operation IDs, component keys, Azure resource IDs or <code>$ref</code>s. IDs are case-sensitive. A local ID must have exactly one kind and, for subscriptions, one parent; <code>topicId != id</code>. Multiple channel aliases of the same entity are allowed only with mutually compatible requirements. A referenced local parent must be a topic. A parent absent from the document is an explicit external logical relation, not a dangling AsyncAPI reference. Composers must resolve it in the shared catalog; do not add unused topic channels just to satisfy it.</p><table tabindex="0"><thead><tr><th><code>deploymentRequirements</code> member</th><th>Type / meaning</th><th>Legal entity</th></tr></thead><tbody><tr><td><code>allowedTiers</code></td><td>Nonempty distinct array of <code>basic</code>, <code>standard</code>, <code>premium</code>; each listed tier must support all known requirements</td><td>All</td></tr><tr><td><code>sessions</code></td><td>Boolean; require enabled/disabled exactly</td><td>Queue, subscription only</td></tr><tr><td><code>partitioning</code></td><td>Only <code>&quot;disabled&quot;</code> in 0.1.0; require nonpartitioned duplicate-detection scope</td><td>Queue, topic</td></tr><tr><td><code>duplicateDetection</code></td><td><code>{required: true, scope: &quot;messageId&quot;, historyWindowSeconds?: integer 20..604800}</code>; require enabled, and an exact history window if supplied</td><td>Queue, topic; requires explicit <code>partitioning: &quot;disabled&quot;</code></td></tr><tr><td><code>lockDurationSeconds</code></td><td>Integer 1..300, exact entity lock duration in seconds</td><td>Queue, subscription</td></tr><tr><td><code>maxDeliveryCount</code></td><td>Integer 1..2147483647, exact broker redelivery limit; not an SDK/business retry count</td><td>Queue, subscription</td></tr><tr><td><code>defaultTtlSeconds</code></td><td>Integer 1..2147483647, exact entity default/ceiling TTL</td><td>All</td></tr><tr><td><code>deadLetterOnExpiration</code></td><td>Boolean, exact deployment requirement</td><td>Queue, subscription</td></tr></tbody></table><p>Numeric units and bounds are fixed. Whole positive seconds, excluding infinite TTL sentinels, are an intentional portable subset. Known Basic requirements reject topics/subscriptions, <code>sessions: true</code>, duplicate detection, or a default TTL over 14 days. With no tier supplied these capabilities remain requirements for Standard/Premium; there is no inferred deployed tier [Z2-Z5, Z8].</p><p>Requirements on aliases combine by conjunction, never last-wins: exact settings must agree; <code>allowedTiers</code> intersect and must remain nonempty. These checks apply within an emitted application. Agreement across separate applications/environments is composition work, not a claim that the emitter has discovered all deployments.</p><h3 id="message-config-and-fixed-native-mappings" tabindex="-1">Message config and fixed native mappings <a class="header-anchor" href="#message-config-and-fixed-native-mappings" aria-label="Permalink to &quot;Message config and fixed native mappings&quot;">​</a></h3><p><code>messageKind</code> optionally classifies <code>command</code>, <code>event</code>, or <code>reply</code>; it does not constrain entity kind. <code>nativeProperties</code> accepts <strong>only</strong> the following PascalCase SDK-neutral names. All declared values are nonempty strings in this profile, even where AMQP or a particular SDK supports more types. Constraints describe wire values, not generated values or payload fields.</p><table tabindex="0"><thead><tr><th>Config key</th><th>Fixed AMQP 1.0 location</th><th>Config value</th></tr></thead><tbody><tr><td><code>MessageId</code></td><td><code>properties/message-id</code></td><td><code>{required: boolean, const?: string, maxLength?: integer}</code>; effective maximum 128</td></tr><tr><td><code>CorrelationId</code></td><td><code>properties/correlation-id</code></td><td>Same shape, no profile-imposed maximum unless supplied</td></tr><tr><td><code>SessionId</code></td><td><code>properties/group-id</code></td><td>Same shape; effective maximum 128</td></tr><tr><td><code>ReplyTo</code></td><td><code>properties/reply-to</code></td><td><code>{required: boolean}</code> only; value supplied by composition/application routing</td></tr><tr><td><code>ReplyToSessionId</code></td><td><code>properties/reply-to-group-id</code></td><td>Native string shape; effective maximum 128</td></tr><tr><td><code>Subject</code></td><td><code>properties/subject</code></td><td>Native string shape, no profile-imposed maximum unless supplied</td></tr><tr><td><code>ContentType</code></td><td><code>properties/content-type</code></td><td><code>{required: true}</code> only; value comes from standard message <code>contentType</code></td></tr><tr><td>Application properties (not native config)</td><td><code>application-properties</code></td><td>Standard message <code>headers</code> schema</td></tr></tbody></table><p>Mappings are fixed by this table, never author-selectable <code>section</code>, <code>name</code> or transformation strings. <code>required: false</code> means optional, not prohibited; all stated constraints still apply if present. The whole native declaration being absent means no requirement, not a generated value. A <code>const</code> longer than a declared <code>maxLength</code> is an error. MessageId, SessionId and ReplyToSessionId have a 128-character profile ceiling grounded in quotas/SDK limits; runtime implementations must also satisfy their SDK&#39;s string-length rules [Z6, Z10]. This RFC does not invent a CorrelationId or Subject limit.</p><p>Explicit standard <code>contentType</code> is required whenever <code>nativeProperties.ContentType</code> is declared. A global AsyncAPI <code>defaultContentType</code> is not an implicit replacement for this per-message declaration. No second ContentType literal is accepted by the profile. Subject is not message <code>name</code>/<code>title</code>. A runtime MessageId is not an operation key/operationId, schema/component name, SessionId or conversation ID. Causation is separately declared in payload or application properties. A send claiming <code>messageIdentity</code> below must require MessageId and generate an identity per logical message, stable across retries; reusing an operation ID is invalid application behavior.</p><p><code>ttlSeconds</code>, if present, declares an exact producer-set per-message TTL, integer 1..4294967. The application maps seconds to AMQP <strong><code>header/ttl</code> milliseconds</strong> (uint32); it is not a property or an application header. The upper bound is this whole-second AMQP subset, not the entity&#39;s maximum retention. Effective TTL can be capped by queue/topic/subscription settings. A known lower ceiling conflicts with the declared exact TTL. Omission leaves per-message TTL unspecified, not automatically set to any broker default [Z4, Z7].</p><p>PartitionKey/scheduling annotations, broker-assigned sequence/enqueue/lock/expiry state, DeliveryCount (<code>header/delivery-count</code>), session state, deferral and transaction settings are not producer config in 0.1.0. Applications may observe them at runtime; the profile neither maps them into headers nor emits them as writable native fields.</p><h3 id="portable-application-properties" tabindex="-1">Portable application properties <a class="header-anchor" href="#portable-application-properties" aria-label="Permalink to &quot;Portable application properties&quot;">​</a></h3><p>Use existing header models, not another property bag in the profile. Resolve standard local schema references before checking: headers must be an object with declared, individually typed scalar properties and <code>additionalProperties: false</code>. Empty headers may be omitted. An unbounded/open header bag is unsupported, even though the core supports more general schemas.</p><table tabindex="0"><thead><tr><th>Header wire value</th><th>Accepted native JSON schema / application encoding</th></tr></thead><tbody><tr><td>String</td><td><code>type: string</code>; constraints/enum allowed; send as AMQP string</td></tr><tr><td>Boolean</td><td><code>type: boolean</code>; send as AMQP boolean</td></tr><tr><td>Integer</td><td><code>type: integer</code> with explicit inclusive <code>minimum</code> and <code>maximum</code> inside -9007199254740991..9007199254740991; send an exactly representable integral number</td></tr><tr><td>Number</td><td><code>type: number</code>; finite IEEE-754 binary64 values only; application checks loss/overflow</td></tr></tbody></table><p>No null, arrays, objects, binary, decimal, unions, implicit date/UUID objects or automatic JSON stringification. A timestamp/UUID/decimal can be deliberately authored as a string with a documented encoding; formats never authorize sending an SDK Date or decimal object. Numeric schemas constrain decoded values, not a particular AMQP integer subtype; applications must preserve the value across SDK encoding/decoding without string-to-number coercion. No default injection. This subset is a cross-SDK project restriction, not a claim that Azure forbids every excluded scalar. Azure quotas still constrain encoded property/message size; a schema cannot prove it [Z6, Z10].</p><p>An application property named <code>CorrelationId</code>, <code>Subject</code>, etc. is legal as a <strong>distinct</strong> application property. Name equality does not turn it into native metadata. Explicit mirrors require the application to write/check both values; the companion must not silently insert, lift, rename or synchronize native fields.</p><h3 id="operation-config-delivery-and-obligations" tabindex="-1">Operation config, delivery and obligations <a class="header-anchor" href="#operation-config-delivery-and-obligations" aria-label="Permalink to &quot;Operation config, delivery and obligations&quot;">​</a></h3><p><code>deliveryRequirements</code> applies only to the operation&#39;s <strong>primary receive</strong>, never implicitly to replies. It is <code>{receiveMode: &quot;peekLock&quot; | &quot;receiveAndDelete&quot;, order?: &quot;perSession&quot;}</code>. A send cannot carry it. If omitted, mode/order are unspecified; omission does not assert a delivery guarantee.</p><p><code>applicationObligations</code> is a closed collection of opt-in requirements with these exact values:</p><table tabindex="0"><thead><tr><th>Member</th><th>Value / applicability</th></tr></thead><tbody><tr><td><code>messageIdentity</code></td><td><code>&quot;uniquePerMessageStableOnRetry&quot;</code>; primary send only</td></tr><tr><td><code>idempotency</code></td><td><code>&quot;required&quot;</code>; receive duplicate handling must avoid repeated business effects according to an application-defined durable policy</td></tr><tr><td><code>settlement</code></td><td><code>&quot;completeAfterSuccess&quot;</code>; complete only after successful effects</td></tr><tr><td><code>lockLoss</code></td><td><code>&quot;treatAsUnsettled&quot;</code>; do not assume a failed/uncertain settlement committed</td></tr><tr><td><code>failureHandling</code></td><td><code>&quot;abandonTransientDeadLetterPermanent&quot;</code> for PeekLock; <code>&quot;applicationRecovery&quot;</code> for ReceiveAndDelete</td></tr><tr><td><code>loss</code></td><td><code>&quot;accepted&quot;</code>; explicit ReceiveAndDelete loss acceptance</td></tr><tr><td><code>sessionProcessing</code></td><td><code>&quot;serial&quot;</code>; one-at-a-time processing within a session</td></tr><tr><td><code>expiry</code></td><td><code>&quot;checkApplicationDeadline&quot;</code> or <code>&quot;processIfDelivered&quot;</code>; choose how delivered/locked but late messages are treated</td></tr><tr><td><code>deadLetter</code></td><td><code>&quot;inspectAndRemediate&quot;</code>; application/operator owns investigation and an explicit discard/replay policy</td></tr></tbody></table><p>All except <code>messageIdentity</code> are receive-only. No settlement, retry or deadline policy is synthesized from omitted fields. Where a mode/order <strong>is declared</strong>, the following conditional requirements are mandatory, not defaults:</p><table tabindex="0"><thead><tr><th>Declared requirement</th><th>Required consistency / effect</th></tr></thead><tbody><tr><td><code>peekLock</code></td><td>Require <code>settlement</code>, <code>lockLoss</code>, <code>failureHandling: abandonTransientDeadLetterPermanent</code>, and <code>idempotency</code>. No <code>loss: accepted</code>. Client owns lock renewal and settlement outcome handling.</td></tr><tr><td><code>receiveAndDelete</code></td><td>Require <code>loss: accepted</code> and <code>failureHandling: applicationRecovery</code>. No settlement, lock-loss policy, per-session order, serial session processing or dead-letter obligation on this operation. Manual settlement/broker-redelivery recovery cannot be promised.</td></tr><tr><td><code>order: perSession</code></td><td>PeekLock plus <code>sessionProcessing: serial</code>, channel <code>sessions: true</code>, and <code>SessionId.required: true</code> on every participating message</td></tr><tr><td>Send to a session-enabled queue, or receive from a session-enabled queue/subscription</td><td>Every participating message requires SessionId; a topic message may carry SessionId for its subscriptions, but the topic itself never has a sessions setting</td></tr><tr><td>Duplicate detection required on sending entity</td><td>Every sent message requires MessageId, and each primary send declares <code>messageIdentity</code>. Apply the same identity obligation to sends represented by replies.</td></tr></tbody></table><p>Replies needing their own receive policies should also have an explicit receive operation on the reply channel; do not apply the request&#39;s receive mode to the inverse reply action. Every reply send remains an application obligation to assign its own MessageId when required, independent of the request&#39;s identity.</p><h3 id="correlation-and-replies" tabindex="-1">Correlation and replies <a class="header-anchor" href="#correlation-and-replies" aria-label="Permalink to &quot;Correlation and replies&quot;">​</a></h3><p><code>nativeReply</code> has exact shape <code>{address: &quot;fixedChannel&quot; | &quot;requestReplyTo&quot;, correlation: &quot;requestMessageId&quot;, session?: &quot;requestReplyToSessionId&quot;}</code>. It is legal only with a standard <code>operation.reply</code> containing explicit <code>channel</code> and nonempty <code>messages</code>. The first profile supports <strong>queue reply destinations only</strong>; Azure&#39;s topic/multicast reply patterns are real but deferred.</p><table tabindex="0"><thead><tr><th>Field</th><th>Normative application obligation / cross-object condition</th></tr></thead><tbody><tr><td><code>correlation: requestMessageId</code></td><td>Request MessageId required; reply CorrelationId required; responder copies request.MessageId to reply.CorrelationId. Each reply&#39;s own MessageId is distinct, not copied.</td></tr><tr><td><code>address: fixedChannel</code></td><td>Route to the standard reply channel after composition. ReplyTo is optional; if sent, it must agree with that composed destination. &quot;Fixed&quot; means a fixed logical queue, whose physical address may still be null in a reusable document.</td></tr><tr><td><code>address: requestReplyTo</code></td><td>Request ReplyTo required; standard reply channel has <code>address: null</code>; application resolves and allowlists ReplyTo to the declared logical reply queue. It does not authorize arbitrary destinations or provision queues.</td></tr><tr><td><code>session: requestReplyToSessionId</code></td><td>Request ReplyToSessionId and reply SessionId required; reply queue requires <code>sessions: true</code>; responder copies the former to the latter</td></tr></tbody></table><p>For native-only metadata, omit standard <code>message.correlationId</code> and <code>reply.address</code>. There is no <code>$message.properties</code> expression and no standardized AMQP-properties-to-JSON-header projection in the current <code>amqp1</code> binding/core. <code>$message.header#/CorrelationId</code> <strong>does not</strong> locate native CorrelationId. A real explicit application-property mirror can instead use that standard expression (encoded wire names and pointer escaping included); a real payload location can use <code>$message.payload#...</code>. Existence, requiredness and scalar shape must be checked, not just expression grammar. These standard locations express the mirrored contract, not automatic native mapping [A1, A2, R1].</p><p>0.1.0 nativeReply modes do not additionally accept standard <code>reply.address</code>; a genuinely application-header-addressed exchange can use core reply facilities without a nativeReply declaration. Both can be valid AsyncAPI, but must not advertise two competing address authorities in this profile. There is no arbitrary extraction/generation DSL: reading an Order.orderId or commandId for SessionId/MessageId, matching replies, validating mirrors, timing out and routing are application code.</p><h2 id="_4-delivery-ownership-and-limits" tabindex="-1">4. Delivery ownership and limits <a class="header-anchor" href="#_4-delivery-ownership-and-limits" aria-label="Permalink to &quot;4. Delivery ownership and limits&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Concern</th><th>Contract / deployment compatibility</th><th>Application / runtime responsibility and limit</th></tr></thead><tbody><tr><td>Settlement</td><td>Explicit mode and conditional obligations above</td><td>PeekLock locks may expire or be lost; completion can fail after effects. Renew while needed (entity lock &lt;=5 minutes); handle uncertain outcomes idempotently. ReceiveAndDelete can lose transferred work. [Z7]</td></tr><tr><td>Duplicate detection</td><td>Queue/topic ingress, enabled over exact window if specified; 20 seconds..7 days; 10-minute Azure default is <strong>not</strong> an emitted default</td><td>Only compares MessageId in this profile&#39;s explicitly nonpartitioned scope, across the whole entity, not per subscription/operation/session. No receive-side or business-effect deduplication. Beyond the window, a resend may be accepted. [Z2]</td></tr><tr><td>Partitioned duplicate detection</td><td>Deferred in 0.1.0; do not claim <code>scope: messageId</code> when partitioned</td><td>Azure can use MessageId + PartitionKey; sessions require compatible SessionId/PartitionKey. This limitation is profile policy, not an impossible Azure configuration. [Z2]</td></tr><tr><td>Order</td><td>Only per-session order on queue/subscription</td><td>Session-lock owner must process serially, including effects. No global/cross-session order; concurrent handlers, dead-letter/replay and resubmission can change observed order. Session state is opaque application data, not workflow durability. [Z3]</td></tr><tr><td>Expiration</td><td>Per-message TTL, entity ceiling/default, and dead-letter-on-expiration are different fields</td><td>Effective topic/subscription ceilings matter. Locked work can outlive nominal expiry; TTL does not cancel business effects. An application deadline needs its own payload/header field and clock policy. [Z4]</td></tr><tr><td>Retries</td><td>No single <code>retryCount</code>; max delivery count only bounds broker delivery attempts</td><td>Distinguish transient SDK send/settlement retry, PeekLock redelivery, and business retry/replay. Send outcome may be uncertain; retain identity for same-send retry. No scheduled retry engine. Not every lock-loss cause increments DeliveryCount. [Z2, Z5, Z7]</td></tr><tr><td>Dead letters</td><td>Queues/subscriptions have built-in DLQ subqueues; no topic DLQ or &quot;enable DLQ&quot; feature</td><td>Exhausted delivery/optional expiration or explicit dead-lettering can move messages there. No automatic TTL cleanup in DLQ. Inspect/remediate/replay explicitly; cannot directly send business messages to a DLQ or dead-letter again from it. Dedicated DLQ operation channels are deferred. [Z5]</td></tr><tr><td>Idempotency</td><td>Opt-in requirement, mandatory with declared PeekLock</td><td>Application chooses durable identity/effect tracking, inbox/outbox or equivalent. Broker filtering and successful settlement do not prove exactly-once business effects or unconditional eventual processing.</td></tr><tr><td>Evolution</td><td>Application/schema/profile versions are separate</td><td>Retain readers for queued/replayed messages and replies across rolling deployments; include payload, app/native metadata, enum/subject semantics and content-type changes. Optional additions are not universally compatible with strict/closed readers. #3 supplies bounded evidence, not a universal proof.</td></tr></tbody></table><p>Changing a required native field, header type, Subject meaning, enum, serialization or reply policy can break old retained messages even when the payload&#39;s JSON shape is unchanged. TypeSpec snapshots neither upgrade stored messages nor rewrite literal profile IDs/strings/runtime expressions.</p><h2 id="_5-security-and-composition" tabindex="-1">5. Security and composition <a class="header-anchor" href="#_5-security-and-composition" aria-label="Permalink to &quot;5. Security and composition&quot;">​</a></h2><p>All profiled AMQP connections require TLS. Optional <code>authorizationRequirements</code> is <code>{tls: true, authentication?: &quot;entraId&quot; | &quot;sas&quot;}</code>; omission leaves authentication choice unspecified, <strong>not TLS optional</strong>. This transport invariant is explicit profile scope, not an injected config default. Primary send requires data-plane Send; primary receive requires Listen. A send-with-reply additionally requires Listen on the reply queue; receive-with-reply requires Send there. These rights follow action and topology, not <code>messageKind</code>.</p><p>Composition supplies physical entity paths, namespace hosts, catalog resolution, deployment capability evidence, identity selection and role assignments. Credentials never belong in reusable contracts or emitted examples. Standard server fields can describe <code>protocol: amqps</code>, <code>protocolVersion: &quot;1.0&quot;</code>; <code>amqp1</code> is the binding name, not that protocol version. Choose <code>@channel</code> once in composition, or retain <code>@dynamicChannel</code>/<code>address: null</code> in an unbound reusable contract. Do not augment a dynamic channel with a second channel decorator. Environment names are not business channel parameters.</p><table tabindex="0"><thead><tr><th>Authentication</th><th>Honest representation</th></tr></thead><tbody><tr><td>Entra ID / managed identity</td><td>Profile requirement plus external identity/RBAC configuration. Standard security schemes do not fully describe managed-identity credential acquisition and AMQP CBS token exchange. Do not fabricate HTTP bearer headers or treat RBAC roles as OAuth scopes.</td></tr><tr><td>Actual OAuth2 client-credentials flow</td><td>Standard <code>oauth2</code> only if that precise flow/endpoints/scopes are actually used and supplied in composition; it still does not describe the entire AMQP CBS exchange</td></tr><tr><td>SAS using SASL PLAIN</td><td>Standard <code>type: plain</code> accurately describes that chosen SASL mechanism; describe policy-name/key usage without embedding either credential. SAS token over CBS is not automatically the same flow.</td></tr></tbody></table><p>When a real standard scheme is defined, reuse existing <code>@securityScheme</code>, <code>@useSecurity</code>, standard server/operation security references. Generic tooling may ignore the extension, so Entra requirements are not fully machine-actionable to every AsyncAPI generator. Never manufacture a scheme to make that limitation disappear [A1, Z9].</p><p>There is no Azure resource inspector or deployment-facts API in this design. Declared incompatible requirements are errors; unknown deployed facts remain external obligations. A future composer comparing real facts must check tier, sessions, partitioning, TTL ceilings, lock/redelivery settings, authentication and rights without claiming the emitter configured them.</p><h2 id="_6-conformance-and-diagnostics" tabindex="-1">6. Conformance and diagnostics <a class="header-anchor" href="#_6-conformance-and-diagnostics" aria-label="Permalink to &quot;6. Conformance and diagnostics&quot;">​</a></h2><p>These are <strong>proposed companion diagnostic identifiers</strong>, not existing compiler diagnostics. The schema fixtures test structural rules; the future companion must implement the cross-object rules at source targets. A profile error prevents a claimed-conformant output, rather than dropping the offending requirement and reporting success.</p><table tabindex="0"><thead><tr><th>Proposed diagnostic</th><th>Severity / kind</th><th>Required trigger</th></tr></thead><tbody><tr><td><code>profile-shape</code></td><td>Error / profile</td><td>Unknown version/member, invalid numeric/string range, unsupported guarantee, nonserializable value, native const exceeds declared bound</td></tr><tr><td><code>profile-placement</code></td><td>Error / profile</td><td>Wrong/unmarked target, target discriminator mismatch, missing info or participating profiles, service/channel dual target</td></tr><tr><td><code>profile-conflict</code></td><td>Error / profile</td><td>Multiple typed configs, typed plus raw same key, action mismatch; no merge or last-wins</td></tr><tr><td><code>topology-conflict</code></td><td>Error / contract</td><td>Kind/parent/alias setting conflict, self-parent, empty tier intersection, invalid local parent</td></tr><tr><td><code>entity-direction</code></td><td>Error / Azure constraint</td><td>Primary or reply direction sends to subscription or receives from topic</td></tr><tr><td><code>transport-conflict</code></td><td>Error / standard/profile</td><td>AMQP 0-9-1/other transport binding, nonempty <code>amqp1</code>, known non-TLS/non-AMQP1 server</td></tr><tr><td><code>native-metadata</code></td><td>Error / profile</td><td>Unsupported/read-only native field, content-type conflict, missing required MessageId/SessionId/correlation/reply metadata</td></tr><tr><td><code>application-properties</code></td><td>Error / profile subset</td><td>Open/nested/nullable/unsupported header shape or encoding, integer without safe bounds</td></tr><tr><td><code>delivery-conflict</code></td><td>Error / contract</td><td>ReceiveAndDelete with manual settlement/order, incomplete PeekLock obligations, session order without capability/metadata</td></tr><tr><td><code>deployment-incompatible</code></td><td>Error / Azure constraint</td><td>Known Basic/capability conflict, impossible lock/window/TTL settings; includes contradictory explicit requirements, not guessed Azure facts</td></tr><tr><td><code>profile-unsupported</code></td><td>Error / profile scope</td><td>Partitioned dedup, non-queue nativeReply, dedicated DLQ channel, unintegrated versioned/multi-service validation; not necessarily impossible in Azure</td></tr><tr><td><code>reply-conflict</code></td><td>Error / contract</td><td>Missing standard reply/channel/messages, competing native/standard addresses, wrong dynamic address, missing correlation/session relation</td></tr><tr><td><code>runtime-location</code></td><td>Error / contract</td><td>Fake native header/property pointer, or declared standard location not backed by an actual required scalar payload/app property</td></tr><tr><td><code>deployment-unverified</code></td><td>Warning / external evidence</td><td>Once per profiled document: emitted requirements have <strong>not</strong> been checked against Azure; identify external topic relations/capabilities to reconcile, no invented observed values</td></tr></tbody></table><p>Schema errors identify JSON paths; typed diagnostics must identify the originating decorator/config argument and related declarations. Do not warn for every omitted optional field. Idempotency, extraction, serialization, routing, settlement and security execution remain documented application obligations, not successful static enforcement.</p><p>Cross-object rules use the final application graph, including resolved references and promoted objects. First release may use separate unversioned entrypoints. Versioned/dependency-mutated or combined multi-service profile claims stay unsupported until #1/#4 provide original Program + selected live namespace/type graph + optional Realm + stable service/version identity + scoped declarations and snapshot-aware relational validation. Source-wide <code>$onValidate</code> is insufficient. Replay must record on the actual cloned target, with per-target/view aggregation, not AST-position/Program-only caches; literals do not migrate automatically.</p><h2 id="_7-examples" tabindex="-1">7. Examples <a class="header-anchor" href="#_7-examples" aria-label="Permalink to &quot;7. Examples&quot;">​</a></h2><p>Download complete, credential-free conceptual AsyncAPI 3.1.0 documents:</p><table tabindex="0"><thead><tr><th>Application</th><th>Document / perspective</th></tr></thead><tbody><tr><td>Gateway</td><td><a href="/profiles/azure-service-bus/0.1.0/gateway.json">gateway.json</a>: sends PlaceOrder to logical <code>orders.commands</code>, fixed <code>orders.replies</code>; explicitly receives results</td></tr><tr><td>Processor</td><td><a href="/profiles/azure-service-bus/0.1.0/processor.json">processor.json</a>: receives PlaceOrder, replies to gateway, sends OrderPlaced to <code>orders.events</code></td></tr><tr><td>Fulfillment</td><td><a href="/profiles/azure-service-bus/0.1.0/fulfillment.json">fulfillment.json</a>: receives OrderPlaced on <code>orders.fulfillment</code>, parent <code>orders.events</code> absent from its channels</td></tr></tbody></table><p>These are <strong>design fixtures, not output from a new companion</strong>. Null addresses are intentionally unbound. The example composition must supply physical names/hosts later. It assigns orderId to command/event SessionId and commandId to reply-session metadata, explicitly in application code; request MessageId is commandId, whereas event/reply MessageIds are new logical-message identities. A <code>causationId</code> application property is distinct from correlation and operation IDs. No native value extraction is performed by the schema.</p><p>Current core can carry raw proposed config without any new API:</p><div class="language-typespec vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typespec</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;tsp-asyncapi&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AsyncAPI</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">@service</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(#{ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">title</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Publisher&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">@info</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(#{ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">version</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;1.0.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">@extension</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;x-azure-service-bus&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, #{</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  profileVersion</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;0.1.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">target</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;info&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">asyncapiVersion</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;3.1.0&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">namespace</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Publisher</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">@jsonSchemaExtension</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;additionalProperties&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">model</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AppProperties</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  causationId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">@message</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">@contentType</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;application/json&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">@headers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">AppProperties</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">@extension</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;x-azure-service-bus&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, #{</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  profileVersion</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;0.1.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">target</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">messageKind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;event&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  nativeProperties</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> #{ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">MessageId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> #{ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">required</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ContentType</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> #{ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">required</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">model</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OrderPlaced</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">orderId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">@dynamicChannel</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">@extension</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;x-azure-service-bus&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, #{</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  profileVersion</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;0.1.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">target</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;channel&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  entity</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> #{ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">kind</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;topic&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;orders.events&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Events</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  @send</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  @extension</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;x-azure-service-bus&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, #{</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    profileVersion</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;0.1.0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">target</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;operation&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">action</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;send&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    applicationObligations</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> #{ </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">messageIdentity</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;uniquePerMessageStableOnRetry&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  })</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  op</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> publish</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">event</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OrderPlaced</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> void</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Generic core checks extension serialization/placement/collisions, <strong>not Service Bus conformance</strong>. <code>@jsonSchemaExtension</code> explicitly closes the application-property schema; no native metadata is inserted there. This snippet proposes no Azure decorators.</p><h3 id="valid-variants-and-invalid-mutations" tabindex="-1">Valid variants and invalid mutations <a class="header-anchor" href="#valid-variants-and-invalid-mutations" aria-label="Permalink to &quot;Valid variants and invalid mutations&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Change to a fixture</th><th>Result</th></tr></thead><tbody><tr><td>Gateway/processor nativeReply <code>address: requestReplyTo</code>, with required native ReplyTo on PlaceOrder and null reply-channel address</td><td>Valid native-only dynamic-routing variant; app must allowlist/composition-resolve ReplyTo; still no standard reply.address</td></tr><tr><td>Add a required string application property <code>requestId</code> and standard <code>correlationId.location: &quot;$message.header#/requestId&quot;</code></td><td>Valid explicit mirror only when the application writes/checks it; does not redefine native CorrelationId</td></tr><tr><td>Add <code>sessions: true</code> to the processor&#39;s topic</td><td>Invalid schema: sessions belong to queue/subscription</td></tr><tr><td>Remove fulfillment entity <code>topicId</code></td><td>Invalid schema: subscription parent required</td></tr><tr><td>Add <code>nativeProperties.MessageId.source: &quot;$message.payload#/commandId&quot;</code></td><td>Invalid schema: no extraction DSL</td></tr><tr><td>Set duplicate window to 19 or 604801 seconds, or omit required nonpartitioned scope</td><td>Invalid schema; valid inclusive endpoints are 20 and 604800</td></tr><tr><td>Change PeekLock to ReceiveAndDelete but retain settlement/order</td><td>Invalid schema; no manual settlement or ordering promise</td></tr><tr><td>Change gateway command action to receive while standard action remains send</td><td>Schema-valid extension, relational <code>profile-conflict</code></td></tr><tr><td>Add <code>bindings.amqp1.bindingVersion: &quot;0.1.0&quot;</code></td><td>Structurally outside the extension schema; <code>transport-conflict</code></td></tr><tr><td>Set only native CorrelationId and then point to <code>$message.header#/CorrelationId</code> without an actual application property</td><td><code>runtime-location</code>; not an interoperable native location</td></tr><tr><td>Topic/subscription with <code>allowedTiers: [&quot;basic&quot;]</code>, or session order with optional SessionId</td><td>Cross-object/capability error, not proof of runtime enforcement</td></tr></tbody></table><p>Executable fixtures use existing Ajv/Vitest and the AsyncAPI parser. They cover schema boundaries, closure, conditional shapes and document structure, not a companion validator, live broker behavior or all relational diagnostics.</p><h2 id="_8-companion-handoff-and-review-gate" tabindex="-1">8. Companion handoff and review gate <a class="header-anchor" href="#_8-companion-handoff-and-review-gate" aria-label="Permalink to &quot;8. Companion handoff and review gate&quot;">​</a></h2><p>Proposed package: <strong><code>tsp-azure-service-bus</code></strong>, namespace <strong><code>Azure.ServiceBus</code></strong>, a TypeSpec library, not an emitter. Exact public TypeSpec decorator names are an implementation-review decision. The reviewed data interface for #2 is the four closed definitions in the schema: one composite config per target, with no separate native/delivery decorators each writing the same key. Native fields are model-level config, not property decorators. Authors use the existing core fields for content type, payload, headers and replies.</p><p>The only necessary new core seam is a narrow <strong>generic extension writer</strong>: actual target, key, plain JSON value and source provenance; same key validation, copy/serialization behavior and deterministic first-source collision diagnostics as raw <code>@extension</code>. Aggregate companion-owned config once per target/view, then write <code>x-azure-service-bus</code> once. Do not deep-import private decorators, mutate core state symbols, change first-wins to merging, add an Azure binding renderer or expand root/server placement. Read-only generic binding inspection may be added only if current public inspection is insufficient for the agreed diagnostics. Any public API must ship in an actual new release before consumers pin it.</p><table tabindex="0"><thead><tr><th>Gate / record</th><th>State</th></tr></thead><tbody><tr><td>Roadmap source analysis, baseline <code>022e4bb</code>, 2026-09-10</td><td>Groundwork only; not maintainer approval</td></tr><tr><td>This RFC + schema + examples</td><td>Reviewed for initial implementation in <a href="https://github.com/cataggar/tsp-asyncapi/pull/7" target="_blank" rel="noreferrer">#7</a>, 2026-09-10</td></tr><tr><td>Placement/closedness/IDs, scalar subset, native/mirror policy, delivery/severity, security, phase limits</td><td>Accepted as the initial implementation contract; public decorator APIs still require review</td></tr><tr><td>Independent design review and approval/merge record</td><td>Automated review found no blockers at <code>7ef1a8f</code>; coordinator accepted the design. The PR records the eventual merge; no human maintainer signoff is asserted.</td></tr><tr><td>#2 companion APIs/runtime validation</td><td>Not implemented here; may begin after this reviewed design is merged</td></tr><tr><td>#6 runnable profile-backed composition</td><td>After approved #5 and implemented #2; separate unversioned app entrypoints recommended</td></tr></tbody></table><p>Before approval, edits may revise the proposed 0.1.0 artifact. After approval/publication, a changed accepted shape or meaning requires a new profile version/schema path; closed-schema readers must explicitly select a supported version. Do not silently accept a new version as 0.1.0. Package release versions evolve independently.</p><h2 id="sources-and-revisions" tabindex="-1">Sources and revisions <a class="header-anchor" href="#sources-and-revisions" aria-label="Permalink to &quot;Sources and revisions&quot;">​</a></h2><p>All references are authoritative upstream sources, reviewed 2026-09-10. Azure prose evolves; the public source snapshot below supplies reproducible revisions rather than claiming these pages are timeless. This RFC summarizes mappings/constraints, not a copy of specification prose.</p><table tabindex="0"><thead><tr><th>ID</th><th>Source / revision</th></tr></thead><tbody><tr><td>A1</td><td><a href="https://github.com/asyncapi/spec/blob/v3.1.0/spec/asyncapi.md" target="_blank" rel="noreferrer">AsyncAPI specification, tag v3.1.0</a>: operation perspective, replies, runtime expressions, schema and security</td></tr><tr><td>A2</td><td><a href="https://github.com/asyncapi/bindings/blob/1f418fe7077096cc8f25d0c6518df617f7c303f3/amqp1/README.md" target="_blank" rel="noreferrer">AMQP 1.0 binding 0.1.0</a>, bindings commit <code>1f418fe7077096cc8f25d0c6518df617f7c303f3</code></td></tr><tr><td>R1</td><td><a href="https://github.com/cataggar/tsp-asyncapi/blob/022e4bb/packages/tsp-asyncapi-core/src/decorators/extension.ts" target="_blank" rel="noreferrer">Core extension implementation</a>, <a href="https://github.com/cataggar/tsp-asyncapi/blob/022e4bb/packages/tsp-asyncapi-core/src/resolve/extensions.ts" target="_blank" rel="noreferrer">resolution</a>, <a href="https://github.com/cataggar/tsp-asyncapi/blob/022e4bb/packages/tsp-asyncapi-core/src/decorators/runtime-expression.ts" target="_blank" rel="noreferrer">runtime expressions</a> at repository baseline <code>022e4bb</code></td></tr><tr><td>T1</td><td><a href="https://github.com/microsoft/typespec/tree/typespec-stable%401.15.0/packages" target="_blank" rel="noreferrer">TypeSpec release tree</a>, tag <code>typespec-stable@1.15.0</code> (<code>f30cd352f93997e04c75d48c7ace6947a1d5d07a</code>): events <code>lib/decorators.tsp</code> / <code>src/experimental</code>, streams README, json-schema emitter/README, versioning mutator and compiler experimental mutators</td></tr><tr><td>Z1</td><td><a href="https://learn.microsoft.com/en-us/azure/service-bus-messaging/service-bus-queues-topics-subscriptions" target="_blank" rel="noreferrer">Queues, topics and subscriptions</a>, <a href="https://learn.microsoft.com/en-us/azure/service-bus-messaging/service-bus-messages-payloads" target="_blank" rel="noreferrer">messages/payloads/routing</a></td></tr><tr><td>Z2</td><td><a href="https://learn.microsoft.com/en-us/azure/service-bus-messaging/duplicate-detection" target="_blank" rel="noreferrer">Duplicate detection</a>: ingress scope, partitioning and history window</td></tr><tr><td>Z3</td><td><a href="https://learn.microsoft.com/en-us/azure/service-bus-messaging/message-sessions" target="_blank" rel="noreferrer">Sessions</a>: queues/subscriptions, ordering and state</td></tr><tr><td>Z4</td><td><a href="https://learn.microsoft.com/en-us/azure/service-bus-messaging/message-expiration" target="_blank" rel="noreferrer">Expiration</a>: TTL ceilings, locked work and expiration handling</td></tr><tr><td>Z5</td><td><a href="https://learn.microsoft.com/en-us/azure/service-bus-messaging/service-bus-dead-letter-queues" target="_blank" rel="noreferrer">Dead-letter queues</a>: built-in subqueues, no TTL cleanup, maximum delivery count</td></tr><tr><td>Z6</td><td><a href="https://learn.microsoft.com/en-us/azure/service-bus-messaging/service-bus-quotas" target="_blank" rel="noreferrer">Quotas</a>: native IDs and encoded sizes; tier capability claims use focused feature pages rather than generic quota headings</td></tr><tr><td>Z7</td><td><a href="https://learn.microsoft.com/en-us/azure/service-bus-messaging/message-transfers-locks-settlement" target="_blank" rel="noreferrer">Transfers, locks and settlement</a>, <a href="https://learn.microsoft.com/en-us/azure/service-bus-messaging/service-bus-amqp-protocol-guide" target="_blank" rel="noreferrer">AMQP protocol guide</a>: property/header/annotation mapping and CBS</td></tr><tr><td>Z8</td><td><a href="https://learn.microsoft.com/en-us/azure/service-bus-messaging/service-bus-premium-messaging" target="_blank" rel="noreferrer">Premium/tier comparison</a></td></tr><tr><td>Z9</td><td><a href="https://learn.microsoft.com/en-us/azure/service-bus-messaging/authenticate-application" target="_blank" rel="noreferrer">Entra application authentication</a></td></tr><tr><td>Z10</td><td><a href="https://github.com/Azure/azure-sdk-for-net/blob/f81988005453a91be7a974bba8c1b69012758e0f/sdk/servicebus/Azure.Messaging.ServiceBus/src/Primitives/ServiceBusMessage.cs" target="_blank" rel="noreferrer">.NET ServiceBusMessage source, v7.20.2 documentation revision</a>: ReplyToSessionId length and application-property types/binary limitation; <a href="https://learn.microsoft.com/en-us/javascript/api/@azure/service-bus/servicebusmessage?view=azure-node-latest" target="_blank" rel="noreferrer">JavaScript message reference</a>, accessed 2026-09-10, not an SDK dependency pin</td></tr></tbody></table><p>Z1-Z9 source files are under <code>articles/service-bus-messaging</code> in <a href="https://github.com/MicrosoftDocs/azure-docs/tree/4c11faea6dc19ae6f860832f98850d434f5f7d62/articles/service-bus-messaging" target="_blank" rel="noreferrer">MicrosoftDocs/azure-docs snapshot <code>4c11faea6dc19ae6f860832f98850d434f5f7d62</code></a>, with the same basename as their Learn URLs. The dedicated DLQ page takes precedence over incidental wording elsewhere suggesting DLQs must be created/enabled.</p>`,83)])])}const m=t(n,[["render",o]]);export{u as __pageData,m as default};
